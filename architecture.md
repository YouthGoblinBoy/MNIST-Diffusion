# UNet 网络结构详解

---

## 一、网络模块概览表

| 模块名称 | 输入通道 | 输出通道 | 主要操作 | 尺寸变化 | 功能说明 |
|---------|---------|---------|---------|---------|---------|
| **时间嵌入** | 1 | 64 | 两层线性变换 + SiLU | - | 将时间步转换为特征向量 |
| **Encoder1** | 1 → 32 | 32 | 两层卷积（3×3） | 不变 | 提取浅层图像特征 |
| **Encoder2** | 32 → 64 | 64 | 两层卷积（第二层步长=2） | 减半 | 提取深层特征并下采样 |
| **中间层** | 64 + 64 | 64 | 两层卷积（3×3） | 不变 | 融合时间信息与深层特征 |
| **Decoder1** | 64 → 32 | 32 | 卷积 + 反卷积 + 卷积 | 翻倍 | 上采样并融合跳连接特征 |
| **Decoder2** | 32 → 1 | 1 | 两层卷积 + 1×1卷积 | 不变 | 输出最终单通道图像 |

---

## 二、时间嵌入模块详解

| 步骤 | 操作 | 细节 |
|------|------|------|
| 1 | 归一化 | `t / (steps-1)`，将时间步映射到 [0, 1] 区间 |
| 2 | 升维 | 通过线性层从 1 维升到 64 维 |
| 3 | 非线性 | 使用 SiLU 激活函数 |
| 4 | 再映射 | 最终输出 64 维时间嵌入向量 |

---

## 三、编码器路径详情

### Encoder1（浅层编码器）
| 层级 | 操作 | 参数 | 激活函数 |
|------|------|------|----------|
| 第1层 | 卷积 | 32通道, 3×3核, padding=1 | ReLU |
| 第2层 | 卷积 | 32通道, 3×3核, padding=1 | ReLU |

### Encoder2（深层编码器）
| 层级 | 操作 | 参数 | 激活函数 |
|------|------|------|----------|
| 第1层 | 卷积 | 64通道, 3×3核, padding=1 | ReLU |
| 第2层 | 卷积+下采样 | 64通道, 3×3核, stride=2 | ReLU |

---

## 四、中间层与时间融合

| 步骤 | 操作 | 说明 |
|------|------|------|
| 1 | 时间嵌入广播 | 将 64 维向量扩展为 14×14 的空间图 |
| 2 | 特征相加 | Encoder2 输出 + 时间特征图 |
| 3 | 卷积处理 | 两层 3×3 卷积提取融合特征 |

**注意**：时间特征被广播到与 Encoder2 输出相同的空间尺寸（14×14）

---

## 五、解码器路径详情

### Decoder1（上采样层）
| 层级 | 操作 | 参数 | 尺寸变化 |
|------|------|------|----------|
| 第1层 | 卷积 | 32通道, 3×3核 | 不变 |
| 第2层 | 反卷积 | 32通道, 4×4核, stride=2 | 翻倍 |
| 第3层 | 卷积 | 32通道, 3×3核 | 不变 |

### Decoder2（输出层）
| 层级 | 操作 | 参数 | 尺寸变化 |
|------|------|------|----------|
| 第1层 | 卷积 | 32通道, 3×3核 | 不变 |
| 第2层 | 卷积 | 32通道, 3×3核 | 不变 |
| 第3层 | 1×1卷积 | 1通道 | 输出单通道图像 |

---

## 六、跳连接映射表

| 跳连接来源 | 融合位置 | 作用 |
|-----------|---------|------|
| Encoder2 输出 | 与中间层输出相加 | 保留深层语义信息 |
| Encoder1 输出 | 与 Decoder1 输出相加 | 保留浅层细节信息 |

---

## 七、完整数据流程图

```
输入图像 (1通道)
    ↓
┌─────────────────┐
│   Encoder1      │ → x1 (32通道, 原尺寸)
│  (浅层编码)     │
└────────┬────────┘
         ↓
┌─────────────────┐
│   Encoder2      │ → x2 (64通道, 尺寸减半)
│  (深层编码)     │
└────────┬────────┘
         ↓
    时间步 t
         ↓
┌─────────────────┐
│  时间嵌入模块   │ → t_embed (64维向量)
└────────┬────────┘
         ↓
    广播为 14×14
         ↓
┌─────────────────┐
│  中间层 (融合)  │ → x3 (64通道)
│ x2 + 时间特征   │
└────────┬────────┘
         ↓
┌─────────────────┐
│   Decoder1      │ → x4 (32通道, 尺寸翻倍)
│ x3 + x2 (跳连)  │
└────────┬────────┘
         ↓
┌─────────────────┐
│   Decoder2      │ → 输出图像 (1通道)
│ x4 + x1 (跳连)  │
└─────────────────┘
```

